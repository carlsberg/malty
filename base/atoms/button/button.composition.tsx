import React from 'react';
import { Button } from './button';
import { ButtonType } from './button.types';

export const BasicButton = () => <Button buttonType={ButtonType.Primary}>click me</Button>;
