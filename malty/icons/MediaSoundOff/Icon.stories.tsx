import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Media Sound Off', Icon);

export default meta;

export const mediaSoundOff = story;
