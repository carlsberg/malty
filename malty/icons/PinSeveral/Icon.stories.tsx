import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Pin Several', Icon);

export default meta;

export const pinSeveral = story;
