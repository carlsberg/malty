import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Calendar Spring', Icon);

export default meta;

export const calendarSpring = story;
