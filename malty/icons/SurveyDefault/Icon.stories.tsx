import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Survey Default', Icon);

export default meta;

export const SurveyDefault = story;
