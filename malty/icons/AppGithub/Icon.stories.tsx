import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('App Github', Icon);

export default meta;

export const appGithub = story;
