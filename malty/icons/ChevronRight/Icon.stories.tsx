import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Chevron Right', Icon);

export default meta;

export const chevronRight = story;
