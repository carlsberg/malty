import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Carret Up', Icon);

export default meta;

export const carretUp = story;
