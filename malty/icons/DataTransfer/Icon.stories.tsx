import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Data Transfer', Icon);

export default meta;

export const dataTransfer = story;
