import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('App Power BI', Icon);

export default meta;

export const appPowerBI = story;
