import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Cart Added', Icon);

export default meta;

export const cartAdded = story;
