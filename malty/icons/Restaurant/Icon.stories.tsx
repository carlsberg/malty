import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Restaurant', Icon);

export default meta;

export const restaurant = story;
