import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Add Event', Icon);

export default meta;

export const addEvent = story;
