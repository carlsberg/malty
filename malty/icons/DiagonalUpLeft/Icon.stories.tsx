import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Diagonal Up Left', Icon);

export default meta;

export const diagonalUpLeft = story;
