import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Speach Balloon Complaint', Icon);

export default meta;

export const speachBalloonComplaint = story;
