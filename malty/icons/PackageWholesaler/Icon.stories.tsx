import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Package Wholesaler', Icon);

export default meta;

export const packageWholesaler = story;
