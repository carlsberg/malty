import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('View Grid', Icon);

export default meta;

export const viewGrid = story;
