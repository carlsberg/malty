import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Chevron Down', Icon);

export default meta;

export const chevronDown = story;
