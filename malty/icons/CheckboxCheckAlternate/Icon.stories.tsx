import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Checkbox Check Alternate', Icon);

export default meta;

export const checkboxCheckAlternate = story;
