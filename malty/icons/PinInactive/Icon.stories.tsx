import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Pin Inactive', Icon);

export default meta;

export const pinInactive = story;
