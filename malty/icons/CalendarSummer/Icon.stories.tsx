import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Calendar Summer', Icon);

export default meta;

export const calendarSummer = story;
