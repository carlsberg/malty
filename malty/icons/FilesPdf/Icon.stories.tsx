import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Files PDF', Icon);

export default meta;

export const filesPDF = story;
