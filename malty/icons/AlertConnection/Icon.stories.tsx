import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Alert Connection', Icon);

export default meta;

export const alertConnection = story;
