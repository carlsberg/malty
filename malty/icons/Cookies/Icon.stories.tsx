import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Cookies', Icon);

export default meta;

export const cookies = story;
