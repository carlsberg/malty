import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Glass Goblet', Icon);

export default meta;

export const glassGoblet = story;
