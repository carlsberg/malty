import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('App Facebook', Icon);

export default meta;

export const appFacebook = story;
