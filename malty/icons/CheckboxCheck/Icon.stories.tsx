import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Checkbox Check', Icon);

export default meta;

export const checkboxCheck = story;
