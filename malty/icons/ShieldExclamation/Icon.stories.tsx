import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Shield Exclamation', Icon);

export default meta;

export const shieldExclamation = story;
