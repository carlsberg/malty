import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Coin Euro', Icon);

export default meta;

export const coinEuro = story;
