import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Carlsberg Filled', Icon);

export default meta;

export const carlsbergFilled = story;
