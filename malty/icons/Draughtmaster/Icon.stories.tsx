import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Draughtmaster', Icon);

export default meta;

export const draughtmaster = story;
