import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Calendar Confirm', Icon);

export default meta;

export const calendarConfirm = story;
