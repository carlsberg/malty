import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Media Pause', Icon);

export default meta;

export const mediaPause = story;
