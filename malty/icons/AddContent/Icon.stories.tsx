import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Add Content', Icon);

export default meta;

export const addContent = story;
