import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Media Play', Icon);

export default meta;

export const mediaPlay = story;
