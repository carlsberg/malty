import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Cart Label', Icon);

export default meta;

export const cartLabel = story;
