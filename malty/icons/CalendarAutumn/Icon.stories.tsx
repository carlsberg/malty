import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Calendar Autumn', Icon);

export default meta;

export const calendarAutumn = story;
