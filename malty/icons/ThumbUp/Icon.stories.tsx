import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Thumb Up', Icon);

export default meta;

export const thumbUp = story;
