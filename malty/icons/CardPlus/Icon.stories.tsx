import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Card Plus', Icon);

export default meta;

export const cardPlus = story;
