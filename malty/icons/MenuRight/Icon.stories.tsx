import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Menu Right', Icon);

export default meta;

export const menuRight = story;
