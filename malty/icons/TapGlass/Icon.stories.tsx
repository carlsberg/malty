import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Tap Glass', Icon);

export default meta;

export const tapGlass = story;
