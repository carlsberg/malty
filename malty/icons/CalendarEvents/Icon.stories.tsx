import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Calendar Events', Icon);

export default meta;

export const calendarEvents = story;
