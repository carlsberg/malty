import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Item Check Filled', Icon);

export default meta;

export const itemCheckFilled = story;
