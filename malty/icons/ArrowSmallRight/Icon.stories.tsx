import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Arrow Small Right', Icon);

export default meta;

export const arrowSmallRight = story;
