import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Glass Mug Empty', Icon);

export default meta;

export const GlassMugEmpty = story;
