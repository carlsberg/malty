import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('App Dropbox', Icon);

export default meta;

export const appDropbox = story;
