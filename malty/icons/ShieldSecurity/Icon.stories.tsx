import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Shield Security', Icon);

export default meta;

export const shieldSecurity = story;
