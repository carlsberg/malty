import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Carret Move', Icon);

export default meta;

export const carretMove = story;
