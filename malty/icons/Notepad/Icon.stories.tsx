import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Notepad', Icon);

export default meta;

export const notepad = story;
