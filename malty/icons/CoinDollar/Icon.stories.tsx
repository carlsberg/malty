import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Coin Dollar', Icon);

export default meta;

export const coinDollar = story;
