import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Radio Button Off', Icon);

export default meta;

export const radioButtonOff = story;
