import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Item Close', Icon);

export default meta;

export const itemClose = story;
