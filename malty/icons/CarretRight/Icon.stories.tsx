import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Carret Right', Icon);

export default meta;

export const carretRight = story;
