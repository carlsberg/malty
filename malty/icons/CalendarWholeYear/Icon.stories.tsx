import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Calendar Whole Year', Icon);

export default meta;

export const calendarWholeYear = story;
