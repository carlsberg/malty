import { IconStories } from '@carlsberggroup/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Clock Filled', Icon);

export default meta;

export const clockFilled = story;
