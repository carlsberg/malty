import { IconStories } from '@carlsberg/malty.atoms.icon-wrapper';
import Icon from './index';

const [meta, story] = IconStories('Diploma', Icon);

export default meta;

export const diploma = story;
