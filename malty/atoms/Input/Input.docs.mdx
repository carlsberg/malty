---
labels: ['react', 'typescript', 'icon']
description: 'A Icon component.'
---

import { IconNamesTypes } from '@carlsberggroup/malty.atoms.icon';
import { Input } from './Input';
import { InputType } from './Input.types';

Icon example:

An simple Icon component with some text

Live Playground:

```js live
<Input type={InputType.Text} value={'This is a test input'} onValueChange={() => null} />
```

```js live
<Input type={InputType.Number} value={0} onValueChange={() => null} />
```

```js live
<Input type={InputType.Email} value={'test@carlsberg.com'} onValueChange={() => null} />
```

```js live
<Input type={InputType.Password} value={'123456'} onValueChange={() => null} />
```

```js live
<Input type={InputType.Date} value={'01/01/1970'} onValueChange={() => null} />
```

```js live
<Input
  type={InputType.Search}
  value={'Test query'}
  icon={IconNamesTypes.Search}
  isIconLeft={true}
  onValueChange={() => null}
/>
```

```js live
<Input type={InputType.Telephone} value={'999-9999'} onValueChange={() => null} />
```

```js live
<Input type={InputType.URL} value={'https://www.domain.com'} onValueChange={() => null} />
```
