---
labels: ['react', 'typescript', 'textarea']
description: 'A textarea component.'
---

import { TextArea } from './TextArea';

TextArea example:

A simple TextArea component with some text

Live Playground:

```js live
<TextArea label={'Texarea label'} value={'This is a texarea'} onValueChange={() => null} />
```
