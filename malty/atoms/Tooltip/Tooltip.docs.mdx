---
labels: ['react', 'typescript', 'text']
description: 'A Tooltip component.'
---

import { Tooltip, Position } from '.';

Text example:

A simple Tooltip component with some text

```js live
<Tooltip position={Position.Top} content={<span>Contents</span>} isOpen>
  <button type="button" onClick={() => null}>
    Toggle
  </button>
</Tooltip>
```

A simple Tooltip component with some text

```js live
<Tooltip position={Position.Right} content={<span>Contents</span>} isOpen>
  <button type="button" onClick={() => null}>
    Toggle
  </button>
</Tooltip>
```

A simple Tooltip component with some text

```js live
<Tooltip position={Position.Bottom} content={<span>Contents</span>} isOpen>
  <button type="button" onClick={() => null}>
    Toggle
  </button>
</Tooltip>
```

A simple Tooltip component with some text

```js live
<Tooltip position={Position.Left} content={<span>Contents</span>} isOpen>
  <button type="button" onClick={() => null}>
    Toggle
  </button>
</Tooltip>
```
