---
labels: ['react', 'typescript', 'checkbox']
description: 'A Checkbox component.'
---

import { Checkbox } from './Checkbox';

Undetermined example:

A simple undetermined checkbox component

```js live
<Checkbox value="Undetermined" labelText="Undetermined" onValueChange={() => null} />
```

A simple `checked={true}` checkbox component

```js live
<Checkbox value="Checked" labelText="Checked" checked={true} onValueChange={() => null} />
```

A simple `checked={false}` checkbox component

```js live
<Checkbox value="Unchecked" labelText="Unchecked" checked={false} onValueChange={() => null} />
```
