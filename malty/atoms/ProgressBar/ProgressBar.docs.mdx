---
labels: ['react', 'typescript', 'button']
description: 'A Progress Bar component.'
---

import { ProgressBar } from '.';

Button example:

A simple Progress Bar component with some text

```js live
<ProgressBar progress={25} displayAmount={false} label="Loading data..." />
```
