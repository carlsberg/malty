export type PrimitiveBorderTokens = {
  default: BorderProps;
};

type BorderProps = {
  style: string;
  width: string;
  color: string;
};

export type SemanticBorderTokens = {
  default: {
    color: string;
  };
};
