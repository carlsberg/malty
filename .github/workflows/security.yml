name: Security
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '30 19 * * 1'
jobs:
  security:
    uses: carlsberggbs/cx-github-workflows/.github/workflows/security.yml@main
    